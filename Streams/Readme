Да се дифинира функция stream-range(a, b), която връща поток, който генерира последователно всички цели числа в затворения интервал [a, b].

Да се дефинира функция stream-ref(s, n), която връща елемента от потока s, който се намира на позиция n, броейки от 0.

Да се дефинира функция stream-map(s, f), която прилага функцията f върху всеки елемент на потока s и връща нов поток.

